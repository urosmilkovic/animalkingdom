import React from 'react';
import { PageLayoutWrapper } from 'layouts/page/styles';

const PageLayout = ({ children }: any) => (
  <PageLayoutWrapper>{children}</PageLayoutWrapper>
);

export default PageLayout;
