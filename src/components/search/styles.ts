import styled from '@emotion/styled';
import { TextField } from '@mui/material';

export const SearchMain = styled(TextField)<any>`
  box-shadow: 0 4px 8px 0 rgb(0 0 0 / 10%), 0 6px 20px 0 rgb(0 0 0 / 9%);
`;
